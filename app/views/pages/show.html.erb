<div class="row">

  <ul class="small-block-grid-1 panel">
    <h1><%= @page.title %></h1>
    <li><%= image_tag(@page.image_url) %></li>
    <small><%= @page.caption %></small>
  </ul>
  <div class="small-block-grid-1">
    <%= render 'navigation' %>
  </div>
</div>
<div>Back to <%= link_to("#{@page.book.title}",@page.book) %></div>
